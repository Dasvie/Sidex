# 보안

취약점을 찾았다면 공개 이슈 대신 GitHub의 **Security → Report a vulnerability**로 비공개로 알려 주세요. 재현 순서와 영향 범위가 있으면 빨리 고칠 수 있어요.

## 지금 지키고 있는 것

- 쓰기 액션은 전부 로그인 세션을 확인하고, 입력은 zod로 검사해요.
- 등록 URL은 DNS를 풀어 공개 주소만 읽어요(사설·링크로컬·localhost 차단).
- 공개 API(`/api/preview`)는 IP별 요청 제한이 있어요. Upstash가 있으면 거기서, 없으면 인스턴스 메모리에서.
- 업로드는 이미지만 받고 sharp로 다시 인코딩해요. 원본은 저장하지 않아요.
- 키와 비밀번호는 리포에 없어요. `.env*`는 훅으로 읽기·쓰기·커밋을 막아요.
- 도우미(`/api/chat`)와 기획 초안(`/api/plan-draft`)은 키가 있을 때만 켜지고, 대화는 저장하지 않아요.
